# ISOLATION TEST. Two rule forms, one deploy, to find out which the platform honours.
# A: a 302 redirect - proves whether this file is parsed and rules fire at all.
/redirect-probe           /guard            302
# B: the 404 rewrites - the thing that did not fire on the last preview.
/llms.txt                 /not-found.html   404
/agents.json              /not-found.html   404
/.well-known/*            /not-found.html   404
# C: a 200 rewrite to the not-found body - does the destination resolve when status is 200?
/rewrite-probe            /not-found.html   200

/*    /index.html   200
